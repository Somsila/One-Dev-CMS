import type { DatabaseAdapter } from '@nuxt/content';
export declare function createDatabaseAdapter<Options = unknown>(factory: (otps: Options) => DatabaseAdapter): (opts: Options) => DatabaseAdapter;
